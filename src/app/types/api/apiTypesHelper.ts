// Для случаев, когда что-то не закрыто генерацией из-за её особенностей или недоработанности документации

// todo перейти на использование сгенерированного интерфейса после исправления документации
export interface OnlineDto {
    online: number;
    max: number;
}
